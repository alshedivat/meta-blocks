defaults:
  # Job logging configuration.
  - hydra/job_logging: meta_blocks_log
  # Data configuration.
  - meta_blocks/data: miniimagent
  # Benchmark configuration.
  - benchmark: ???
  - backbone: ???
  - method: ???
