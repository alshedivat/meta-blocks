# Proto adaptation at training and test time.
meta_blocks:
  train:
    model:
      name: proto
      embedding_dim: 64
    adapt:
      name: proto
    optimizer:
      name: adam
      learning_rate: 0.002
  eval:
    model:
      name: proto
      embedding_dim: 64
    adapt:
      name: proto
