# First-order MAML adaptation at training and test time.
meta_blocks:
  train:
    adapt:
      name: maml
      first_order: true
      num_inner_steps: 4
      inner_optimizer:
        name: sgd
        learning_rate: 0.002
    optimizer:
      name: adam
      beta1: 0.
      learning_rate: 0.005
  eval:
    adapt:
      name: maml
      first_order: true
      num_inner_steps: 8
      inner_optimizer:
        name: sgd
        learning_rate: 0.002
