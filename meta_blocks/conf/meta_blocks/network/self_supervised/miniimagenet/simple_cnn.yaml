meta_blocks:
  network:
    name: simple_cnn
    filters: [32, 32, 32, 32]
    kernel_size: 3
    conv2d_kwargs:
      strides: 2
      padding: same
    pooling: max
    pooling_kwargs:
      pool_size: [2, 2]
      padding: same
    activation: relu
    batch_norm:
      center: true
      scale: true